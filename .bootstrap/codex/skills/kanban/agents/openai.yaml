interface:
  display_name: "Kanban"
  short_description: "Work GitHub project issues and threads"
  default_prompt: "Use $kanban to pick the next issue linked to my GitHub Project, read the full issue thread, turn it into a stronger execution plan, then start working and leave concise human ticket updates only when work starts, finishes, or needs attention."

policy:
  allow_implicit_invocation: true
