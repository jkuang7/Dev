# Tests Exemption

Update this file in your PR only when `pnpm run tests:changed` needs a temporary exemption.

Required fields:
- Scope of source changes
- Why co-located unit tests are not feasible in this PR
- Follow-up issue/PR to add tests
